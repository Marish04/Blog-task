const menuBtn=document.querySelector(".burger"),menu=document.querySelector(".menu");let menuOpen=!1;menuBtn.addEventListener("click",()=>{menuOpen?(menuBtn.classList.remove("open"),menu.classList.remove("open"),menuOpen=!1):(menuBtn.classList.add("open"),menu.classList.add("open"),menuOpen=!0)});